# Plan 04-02 — Firestore Vector Search & Incident Memory RAG

**Phase**: 04 — Google Cloud Platform & ADK Fleet Modernization
**Plan**: 04-02
**Wave**: 1
**Depends on**: None
**Objective**: Implement dense semantic incident retrieval using Firestore Vector Search and text-embedding-004 with local in-memory vector search fallback.

## Proposed Changes

### Incident Memory & Embeddings

#### [MODIFY] [incident_memory.py](file:///home/zeph/Code/deployguard/src/deployguard/agents/incident_memory.py)
- Add synchronous vector embedding generation via `text-embedding-004` (using `google-genai` embeddings API) during incident ingestion and resolution.
- Implement pre-filtered Firestore Vector Search query using `VectorQuery` / `find_nearest(vector_field="embedding", distance_measure=COSINE)` after filtering by `service_name` and `environment`.
- Enforce configurable top-k ($k=3$) and cosine similarity threshold ($\ge 0.70$) to filter out irrelevant historical noise.

#### [MODIFY] [stubs.py](file:///home/zeph/Code/deployguard/src/deployguard/cloud/stubs.py)
- Upgrade `MockFirestore` to support in-memory dense vector cosine distance calculation (`find_nearest`) on stored documents for full offline test parity.

### Tests

#### [NEW] [test_vector_search.py](file:///home/zeph/Code/deployguard/tests/test_vector_search.py)
- Unit tests for synchronous embedding vector generation.
- Unit tests for pre-filtered vector similarity search and similarity threshold filtering in `MockFirestore` and `IncidentMemoryAgent`.

## Verification Plan

### Automated Tests
- `pytest tests/test_vector_search.py`

### Manual Verification
- None.
