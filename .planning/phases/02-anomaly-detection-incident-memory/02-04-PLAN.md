---
phase: 02-anomaly-detection-incident-memory
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/deployguard/security/sanitizer.py
  - src/deployguard/agents/incident_memory.py
  - tests/test_sanitizer.py
autonomous: true
requirements: ["MEM-03", "SEC-01"]
user_setup: []
must_haves:
  truths:
    - "`pytest tests/test_sanitizer.py` passes"
  artifacts:
    - path: "src/deployguard/security/sanitizer.py"
      provides: "Multi-stage regex + keyword log sanitizer"
      contains: "LogSanitizer"
---

<objective>
Implement the Log Sanitization Layer to redact PII and filter out potential prompt injection attempts from logs.
This includes:
- Creating `src/deployguard/security/sanitizer.py` implementing `LogSanitizer`.
- Integrating `LogSanitizer` in the `IncidentMemoryAgent` to sanitize logs before persisting or passing to other components.
- Writing unit tests in `tests/test_sanitizer.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-anomaly-detection-incident-memory/02-CONTEXT.md
@.planning/phases/02-anomaly-detection-incident-memory/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the LogSanitizer class</name>
  <files>src/deployguard/security/sanitizer.py</files>
  <action>
Create `src/deployguard/security/sanitizer.py` with `LogSanitizer`:
- Stage 1: Regex patterns to match and redact PII (emails, IPv4 addresses, API keys, database credentials, passwords, bearer tokens).
- Stage 2: Keyword scans for typical prompt injection attacks (case-insensitive checks for phrases like "ignore previous instructions", "system override", "you are now an admin", "bypass security").
- Method `sanitize(self, log_entry: str) -> str` returns the sanitized log entry, replacing matched items with redaction markers.
  </action>
  <verify>python -c "from deployguard.security.sanitizer import LogSanitizer; print('LogSanitizer imported')"</verify>
  <acceptance_criteria>
    - `LogSanitizer` is implemented and can redact email addresses and IP addresses
    - Replaces prompt injection keywords with warning markers
  </acceptance_criteria>
  <done>LogSanitizer class implemented</done>
</task>

<task type="auto">
  <name>Task 2: Integrate LogSanitizer with IncidentMemoryAgent</name>
  <files>src/deployguard/agents/incident_memory.py</files>
  <action>
Modify `IncidentMemoryAgent` in `src/deployguard/agents/incident_memory.py` to instantiate `LogSanitizer` and pass raw log payload through `sanitize()` before storing it in Firestore or returning it.
  </action>
  <verify>python -c "from deployguard.agents.incident_memory import IncidentMemoryAgent; print('IncidentMemoryAgent compiles')"</verify>
  <acceptance_criteria>
    - `IncidentMemoryAgent` sanitizes input logs before document store writes
  </acceptance_criteria>
  <done>LogSanitizer integrated with IncidentMemoryAgent</done>
</task>

<task type="auto">
  <name>Task 3: Create sanitizer unit tests</name>
  <files>tests/test_sanitizer.py</files>
  <action>
Create `tests/test_sanitizer.py` to verify:
- Redaction of emails (e.g. `user@example.com` -> `[REDACTED]`).
- Redaction of IPv4 addresses.
- Redaction of credentials (e.g. `api_key = "secret_123"`).
- Redaction and marking of prompt injection attempts (e.g. log containing "ignore previous instructions").
- Integration test checking that `IncidentMemoryAgent` persists the redacted log, not the raw one.
  </action>
  <verify>pytest tests/test_sanitizer.py</verify>
  <acceptance_criteria>
    - `tests/test_sanitizer.py` exists
    - All log sanitization unit and integration tests pass
  </acceptance_criteria>
  <done>Sanitizer tests created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_sanitizer.py` passes
- [ ] `ruff check src/ tests/` passes with 0 errors
</verification>

<success_criteria>
- LogSanitizer redacts PII and credentials using regex
- LogSanitizer neutralizes prompt injection signatures
- IncidentMemoryAgent persists only sanitized logs in Firestore
</success_criteria>

<output:SUMMARY>
After completion, create `.planning/phases/02-anomaly-detection-incident-memory/02-04-SUMMARY.md`
</output:SUMMARY>
