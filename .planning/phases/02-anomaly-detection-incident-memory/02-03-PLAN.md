---
phase: 02-anomaly-detection-incident-memory
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/deployguard/cloud/interfaces.py
  - src/deployguard/cloud/stubs.py
  - src/deployguard/agents/incident_memory.py
  - tests/test_incident_memory.py
autonomous: true
requirements: ["MEM-01"]
user_setup: []
must_haves:
  truths:
    - "`pytest tests/test_incident_memory.py` passes"
  artifacts:
    - path: "src/deployguard/cloud/interfaces.py"
      provides: "Typing protocol for Firestore DocumentStore"
      contains: "DocumentStore"
    - path: "src/deployguard/agents/incident_memory.py"
      provides: "Incident Memory Agent leveraging DocumentStore for persistence"
      contains: "IncidentMemoryAgent"
---

<objective>
Implement Firestore document store integration for the Incident Memory Agent.
This includes:
- Defining the `DocumentStore` protocol interface in `src/deployguard/cloud/interfaces.py`.
- Refactoring `MockFirestore` stub in `src/deployguard/cloud/stubs.py` to conform to `DocumentStore` with async methods.
- Updating `IncidentMemoryAgent` to load and save incident traces, deployment events, and decisions using `DocumentStore`.
- Writing unit and integration tests in `tests/test_incident_memory.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-anomaly-detection-incident-memory/02-CONTEXT.md
@.planning/phases/02-anomaly-detection-incident-memory/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define DocumentStore Protocol</name>
  <files>src/deployguard/cloud/interfaces.py</files>
  <action>
Extend `src/deployguard/cloud/interfaces.py` to define the `DocumentStore` protocol:

```python
class DocumentStore(Protocol):
    async def set_document(self, collection: str, document_id: str, data: Dict[str, Any]) -> None: ...
    async def get_document(self, collection: str, document_id: str) -> Dict[str, Any] | None: ...
    async def query(self, collection: str, filters: List[tuple]) -> List[Dict[str, Any]]: ...
```
  </action>
  <verify>python -c "from deployguard.cloud.interfaces import DocumentStore; print('DocumentStore imported')"</verify>
  <acceptance_criteria>
    - `DocumentStore` protocol exists in `interfaces.py`
  </acceptance_criteria>
  <done>DocumentStore protocol defined</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MockFirestore to implement DocumentStore</name>
  <files>src/deployguard/cloud/stubs.py</files>
  <action>
Update `MockFirestore` in `src/deployguard/cloud/stubs.py` to conform to `DocumentStore`:
- All storage methods must be `async def`.
- Implement `query()` supporting basic filtering (e.g. matching tuples like `(field, "==", value)`).
  </action>
  <verify>pytest tests/test_cloud.py</verify>
  <acceptance_criteria>
    - `MockFirestore` methods are async
    - Basic query filtering is verified by existing test_cloud
  </acceptance_criteria>
  <done>MockFirestore refactored to implement DocumentStore</done>
</task>

<task type="auto">
  <name>Task 3: Update IncidentMemoryAgent</name>
  <files>src/deployguard/agents/incident_memory.py</files>
  <action>
Refactor `IncidentMemoryAgent` in `src/deployguard/agents/incident_memory.py`:
- Use `DocumentStore` (injected or stubbed) to write deployment events and incident logs.
- Support storing a workflow trace: write document to collections `"incidents"` with schema:
  `{deployment_id, service_name, status, version, events: [...]}`.
- Support looking up historical incidents: query `"incidents"` collection for past incidents matching the same `service_name` or with failure outcomes.
- Yield an Event detailing any historical incidents found.
  </action>
  <verify>python -c "from deployguard.agents.incident_memory import IncidentMemoryAgent; print('IncidentMemoryAgent compiles')"</verify>
  <acceptance_criteria>
    - `IncidentMemoryAgent` stores workflow details to `DocumentStore`
    - Queries for previous incidents and reports findings via yielded Event
  </acceptance_criteria>
  <done>IncidentMemoryAgent Firestore integration completed</done>
</task>

<task type="auto">
  <name>Task 4: Write tests for IncidentMemoryAgent</name>
  <files>tests/test_incident_memory.py</files>
  <action>
Create `tests/test_incident_memory.py` to verify:
- Instantiating and executing `IncidentMemoryAgent` with a populated `MockFirestore`.
- Confirming that the agent writes current deployment data to the mock db.
- Confirming that the agent retrieves past incidents and yields a correct historical summary event.
  </action>
  <verify>pytest tests/test_incident_memory.py</verify>
  <acceptance_criteria>
    - `tests/test_incident_memory.py` exists
    - Tests verify write and lookup functionality
  </acceptance_criteria>
  <done>Incident memory unit tests created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_incident_memory.py` passes
- [ ] `pytest tests/test_cloud.py` passes
- [ ] `ruff check src/ tests/` passes with 0 errors
</verification>

<success_criteria>
- IncidentMemoryAgent writes to Firestore via DocumentStore protocol
- Historic incident lookup retrieves past occurrences correctly
- Memory storage schema matches requirements
</success_criteria>

<output:SUMMARY>
After completion, create `.planning/phases/02-anomaly-detection-incident-memory/02-03-SUMMARY.md`
</output:SUMMARY>
