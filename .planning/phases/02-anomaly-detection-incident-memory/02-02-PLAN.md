---
phase: 02-anomaly-detection-incident-memory
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/deployguard/agents/deploy_monitor.py
  - tests/test_deploy_monitor.py
autonomous: true
requirements: ["ANOM-02", "ANOM-03"]
user_setup: []
must_haves:
  truths:
    - "`pytest tests/test_deploy_monitor.py` passes"
  artifacts:
    - path: "src/deployguard/agents/deploy_monitor.py"
      provides: "Real anomaly detection agent leveraging compare_metrics and MetricsSource"
      contains: "DeployMonitorAgent"
---

<objective>
Integrate the ratio-threshold engine into the Deploy Monitor Agent.
This includes:
- Updating `DeployMonitorAgent` to retrieve current metrics and baselines from `MetricsSource` (injected via ctx).
- Invoking `compare_metrics` on the results.
- Generating the structured `AnomalySignal` if any anomaly is detected, updating `DeploymentWorkflowState`, and yielding appropriate events.
- Writing integration tests in `tests/test_deploy_monitor.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-anomaly-detection-incident-memory/02-CONTEXT.md
@.planning/phases/02-anomaly-detection-incident-memory/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DeployMonitorAgent logic</name>
  <files>src/deployguard/agents/deploy_monitor.py</files>
  <action>
Update `DeployMonitorAgent` in `src/deployguard/agents/deploy_monitor.py` to:
- Accept `metrics_source: MetricsSource` in constructor or fetch from context/config.
- Query `get_metric()` and `get_baseline()` for the seven monitored metrics.
- Compute delta using `compare_metrics()`.
- Populate `baseline_metrics` and `current_metrics` in `DeploymentWorkflowState`.
- Determine severity ("CRITICAL" if any critical metrics fail, "HIGH" if multiple high anomalies, etc.).
- Populate `AnomalySignal` in `DeploymentWorkflowState` and set `pipeline_status = "anomaly_detected"`.
- Yield Event representing completion/signals.
  </action>
  <verify>python -c "from deployguard.agents.deploy_monitor import DeployMonitorAgent; print('DeployMonitorAgent compiles')"</verify>
  <acceptance_criteria>
    - `DeployMonitorAgent` queries metrics via the injected source
    - Computes delta ratio using `compare_metrics`
    - Sets state attributes appropriately on anomaly detection
  </acceptance_criteria>
  <done>DeployMonitorAgent logic updated</done>
</task>

<task type="auto">
  <name>Task 2: Write agent integration tests</name>
  <files>tests/test_deploy_monitor.py</files>
  <action>
Update `tests/test_deploy_monitor.py` to test the agent invocation:
- Setup a `MockMonitoring` instance with healthy and anomalous data.
- Setup `InvocationContext` with `DeploymentWorkflowState`.
- Instantiate and execute `DeployMonitorAgent`.
- Verify the yielded Event content and the mutation of `DeploymentWorkflowState` in the session.
  </action>
  <verify>pytest tests/test_deploy_monitor.py</verify>
  <acceptance_criteria>
    - Tests cover both healthy and anomalous scenarios for the agent
    - WorkflowState is mutated correctly on session state
  </acceptance_criteria>
  <done>Agent integration tests created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_deploy_monitor.py` passes
- [ ] `ruff check src/ tests/` passes with 0 errors
</verification>

<success_criteria>
- DeployMonitorAgent correctly identifies anomalies
- AnomalySignal contains severity, evidence list, affected metrics, and confidence
- WorkflowState is updated correctly in the context session
</success_criteria>

<output:SUMMARY>
After completion, create `.planning/phases/02-anomaly-detection-incident-memory/02-02-SUMMARY.md`
</output:SUMMARY>
