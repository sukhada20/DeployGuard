# Plan 03-01 — Agent Gateway & Policy Evaluation

**Phase**: 03 — Decisioning Engine & Governance
**Plan**: 03-01
**Wave**: 1
**Depends on**: None
**Objective**: Build the Agent Gateway to verify agent credentials against the Registry, and implement the config-driven JSON policy engine.

## Proposed Changes

### Security

#### [NEW] [gateway.py](file:///c:/Users/sukha/Downloads/DeployGuard/src/deployguard/security/gateway.py)
- Create `AgentGateway` class initialized with Agent Registry and Firestore database.
- Create `PolicyEngine` class that loads JSON configuration and checks:
  - Allowed severities per environment (e.g. staging allows MEDIUM/HIGH/CRITICAL; production allows CRITICAL only).
  - Version presence (requires a non-empty `rollback_target_version`).
  - Minimum confidence threshold (e.g. confidence score >= 0.8).
- Implement `authorize_action(agent_id, action)` checking registry permissions matrix.

### Tests

#### [NEW] [test_gateway.py](file:///c:/Users/sukha/Downloads/DeployGuard/tests/test_gateway.py)
- Unit tests for `PolicyEngine` evaluating healthy/failing policies using config templates.
- Unit tests for `AgentGateway` authorizing active agents with correct permissions and rejecting invalid/deactivated ones.

## Verification Plan

### Automated Tests
- `pytest tests/test_gateway.py`

### Manual Verification
- None.
