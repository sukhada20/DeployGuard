# Plan 03-03 — Decision Agent Integration

**Phase**: 03 — Decisioning Engine & Governance
**Plan**: 03-03
**Wave**: 2
**Depends on**: 03-02
**Objective**: Build the core Decision Agent logic to fetch historical context, poll the Gemini LLM for recommendations, run policies, and mutate workflow status.

## Proposed Changes

### Agents

#### [MODIFY] [decision.py](file:///c:/Users/sukha/Downloads/DeployGuard/src/deployguard/agents/decision.py)
- Refactor `DecisionAgent` class initialization to accept `GeminiReasoningClient` and `PolicyEngine` (injectable for testing).
- Implement `_execute(ctx)` logic:
  - Retrieve current `DeploymentWorkflowState` from the session.
  - Query Firestore via `DocumentStore` for prior historical incidents of the same service (replicating/invoking lookup context).
  - Aggregate anomaly signals, metrics, and logs.
  - Query `GeminiReasoningClient` to fetch a recommendation (`rollback` / `wait` / `alert`).
  - Run the gathered context and recommendation through `PolicyEngine`.
  - Mutate state:
    - Set `decision_trace` with policies check details, confidence, reason.
    - Set `pipeline_status` to `"decision_made"`.
  - Persist workflow state and yield Event.

### Tests

#### [MODIFY] [test_decision.py](file:///c:/Users/sukha/Downloads/DeployGuard/tests/test_decision.py)
- Add integration tests executing `DecisionAgent` with mock incidents and verification of the generated decision trace and status mutation.

## Verification Plan

### Automated Tests
- `pytest tests/test_decision.py`

### Manual Verification
- None.
