# Plan 06-04 — Decision Trace Stepper, Postmortem Viewer, Fleet Registry & Static Serving

**Phase**: 06 — Postmortem Generation & Operator Dashboard
**Plan**: 06-04
**Wave**: 2
**Depends on**: 06-01, 06-02, 06-03
**Objective**: Implement the remaining 3 dashboard views: (1) GSAP-choreographed Decision Trace Governance Stepper with OpenTelemetry span waterfall, (2) Rich SRE Postmortem document viewer with one-click Markdown/JSON export, (3) Agent Fleet Registry with IAM capability matrix and AnimatedBeam connections, and configure FastAPI static SPA serving for single-command production deployment.

## Proposed Changes

### Visual Governance Stepper, Postmortem Viewer & Fleet Registry

#### [NEW] [DecisionTraceStepper.tsx](file:///home/zeph/Code/deployguard/web/src/components/traces/DecisionTraceStepper.tsx)
- Multi-step horizontal governance stepper:
  - Step 1: Anomaly Signal & Telemetry Evidence.
  - Step 2: Vertex AI RAG / Vector Memory Analogues.
  - Step 3: Gemini 2.5 Flash Reasoning & Model Armor Sanitization.
  - Step 4: Deterministic Policy Gate (5 rules evaluated with pass/fail chips).
  - Step 5: Agent Gateway Authorization (IAM identity verification).
  - Step 6: Rollback Execution Plan.
- Use GSAP (`@gsap/react`) for fluid timeline transitions, step-by-step reveals, and glowing connection paths.

#### [NEW] [SpanWaterfall.tsx](file:///home/zeph/Code/deployguard/web/src/components/traces/SpanWaterfall.tsx)
- OpenTelemetry Gantt span visualizer rendering `deployguard.deployment` root span and child execution spans (`monitor.detect`, `decision.evaluate`, `rollback.execute`, `monitor.verify_recovery`, `postmortem.generate`) with execution duration bars and deep links to GCP Cloud Trace.

#### [NEW] [PostmortemDocumentViewer.tsx](file:///home/zeph/Code/deployguard/web/src/components/postmortem/PostmortemDocumentViewer.tsx)
- High-polish SRE Incident Report layout:
  - Executive summary callout box with severity badges.
  - 5-Whys Root Cause Analysis section.
  - Metric delta comparison table with anomaly highlights.
  - Rollback outcome and recovery verification verdict.
  - Preventative action checklist.
  - One-click action buttons: "Download .md", "Download JSON", "Copy Markdown".

#### [NEW] [FleetRegistryView.tsx](file:///home/zeph/Code/deployguard/web/src/components/registry/FleetRegistryView.tsx)
- 5 Agent status cards (Deploy Monitor, Decision, Memory, Rollback, Postmortem) with role badges, assigned ADK tools, security risk levels, and health status indicators.
- Interactive IAM Capability Matrix table showing permissions across Cloud Deploy, Cloud Monitoring, Cloud Logging, Firestore, and Gateway tools.

#### [NEW] [AnimatedBeam.tsx](file:///home/zeph/Code/deployguard/web/src/components/ui/AnimatedBeam.tsx) & [BorderBeam.tsx](file:///home/zeph/Code/deployguard/web/src/components/ui/BorderBeam.tsx)
- SVG animated bezier beam component showing dynamic agent-to-gateway message traffic.
- Animated glowing border box for active incident alerts.

#### [MODIFY] [main.py](file:///home/zeph/Code/deployguard/src/deployguard/main.py)
- Mount static files from `web/out` (or `web/dist`) to serve the built Next.js SPA on `/` when the directory exists, ensuring single-command backend + frontend serving.

### Verification Plan

#### Automated Tests
- `pytest tests/ -v` (Full backend test suite)
- `npm --prefix web run build` (Next.js static export build)

#### Manual Verification
- Run `obscura` or open browser at `http://localhost:8000` / `http://localhost:3000`:
  - Verify all 4 tabs (Live Operations, Decision Traces, Postmortem Viewer, Fleet Registry) render properly.
  - Verify Decision Trace governance stepper displays with GSAP animations.
  - Verify Postmortem Viewer renders formatted report and downloads Markdown correctly.
  - Verify Fleet Registry renders all 5 agents and IAM matrix.
